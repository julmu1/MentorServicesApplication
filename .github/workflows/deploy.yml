# name: Django CI/CD Pipeline

# on:
#   push:
#     branches:
#     - develop

# jobs:
#   test:
#     runs-on: ubuntu-latest
#     steps: 

#       - name: Checkout Code
#         uses: actions/checkout@v3
#         #debugging Step: List directory contents
#       - name: Debug: List root directory
#         run: ls -l
#         working-directory: .

#       - name: Set up Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: '3.9'

#       - name: Install dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt
#         working-directory: .

#       - name: Run Tests with Coverage
#         run: |
#           pytest -cov=. --cov-report=xml

#       - name: Upload Coverage to Codecov
#         uses: codecov/codecov-action@v3
#         with:
#           files: ./coverage.xml
        
#   deploy: 
#     needs: test
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v3

#       - name: Install Render CLI
#         run: curl -fsSL https://render.com/deploy | bash

#       - name: Deploy to Render
#         env:
#           RENDER_API_KEY: ${{ secrets.RENDER_API_KEY}}
#         run: render deploy

########################

# name: Django CI/CD Pipeline

# on:
#   push:
#     branches:
#       - develop

# jobs:
#   test:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v3

#       - name: Set up Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: '3.9'

#       - name: Install dependencies
#         run: |
#           python3 -m pip install --upgrade pip
#           pip install -r requirements.txt

#       - name: Run Tests with Coverage
#         run: |
#           pytest --cov=. --cov-report=xml

#       - name: Upload Coverage to Codecov
#         uses: codecov/codecov-action@v3
#         with:
#           files: ./coverage.xml
        
#   deploy: 
#     needs: test
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v3

#       - name: Install Render CLI
#         run: curl -fsSL https://render.com/deploy | bash

#       - name: Deploy to Render
#         env:
#           RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
#         run: render deploy


        #######

name: Django CI/CD Workflow

on:
  push:
    branches:
      - develop  # Run workflow on push to 'develop' branch
  pull_request:
    branches:
      - develop  # Run workflow for pull requests targeting 'develop'

jobs:
  test-and-deploy:
    runs-on: ubuntu-latest

    steps:
    # Step 1: Check out the repository
    - name: Checkout code
      uses: actions/checkout@v3

    # Step 2: Set up Python environment
    - name: Set up Python 3.11
      uses: actions/setup-python@v4
      with:
        python-version: 3.11

    # Step 3: Install dependencies
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    # Step 4: Run database migrations
    - name: Run database migrations
      run: |
        python manage.py makemigrations
        python manage.py migrate

    # Step 5: Run tests
    - name: Run tests
      run: |
        python manage.py test

    # Step 6: Deploy to Render
    - name: Deploy to Render
      uses: render-examples/deploy-action@v1
      env:
        RENDER_SERVICE_ID: ${{ secrets.RENDER_SERVICE_ID }}
        RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}